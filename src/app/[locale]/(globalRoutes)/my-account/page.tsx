import AccountInfo from '@/app/[locale]/(globalRoutes)/my-account/_components/AccountInfo/AccountInfo';

export default function myAccount() {
  return (
    <main>
      <AccountInfo />
    </main>
  );
}
